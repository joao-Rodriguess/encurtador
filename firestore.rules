rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /links/{linkId} {
      // ABERTO TOTALMENTE PARA TESTE DE CONEXÃO
      allow read, write: if true;
    }
  }
}
